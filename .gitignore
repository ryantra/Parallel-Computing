# Save this as ".gitignore" at the root of the Parallel-Computing repo
# (CMake project). Your build/ directory is currently committed — after
# adding this, run:  git rm -r --cached build

# --- CMake build tree ---
build/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile         # generated by CMake; remove this line if you hand-write a Makefile

# --- Compiled objects and binaries ---
*.o
*.a
*.so
*.out
parallel         # the built executable

# --- OS noise ---
.DS_Store
Thumbs.db
